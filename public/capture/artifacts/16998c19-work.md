**Artifact path:** `launch-plan.md` (sha256 `a9ee567d6c2edfabc2d26788eb6528311d9f61ad44f22db7a01433b95a55d63f`)

**Summary of stop/go criteria:**
- **GO** only if all P0 checks pass: injection defense (0/50 payloads trigger unauthorized tool calls; 100% out-of-allowlist requests blocked), privacy (0 PII tokens in 100 sampled logs; 100% deletion within 24h of the 30-day window), and human approval (0/20 refund/account/deletion requests execute without approval).
- **STOP** on any P0 miss, or if the kill-switch exceeds 60s to revert to human-only routing.
- **Partial GO** for P1/P2 misses (reliability fallback, escalation, backlog callback) with documented mitigations and a rollback path.

**Evidence:** Combined both dependency artifacts read via `read_artifact` — reliability-recovery.md (failure modes, retry/timeout, escalation, kill-switch, 5 proposed tests) and security-privacy-approval.md (injection defenses, privacy, approval gates, 5 proposed tests). The plan includes prioritized P0/P1/P2 checks, stop/go criteria tied to all 10 worker acceptance tests, an explicit Assumptions section (all facts fictional, no production testing), and a clear "proposed, not executed" label.

**Blocker:** None. No web research, integrations, or external actions were used, per assignment constraints.